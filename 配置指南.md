# 朋友圈分析交友助手 - 完整配置指南

## 🚨 问题诊断结果

**当前错误**: 401 AuthenticationError - API Key 格式不正确

---

## 📝 完整配置步骤（必须按顺序完成）

### 第 1 步：获取正确的 API Key

1. 访问火山方舟控制台 API 密钥管理：
   https://console.volcengine.com/ark/region:ark+cn-beijing/apikey

2. 点击"创建 API Key"

3. **重要**：复制完整的 API Key（格式通常是 `ak-` 开头的长字符串）

4. 不要复制页面上显示的其他 ID！

---

### 第 2 步：部署模型，获取 Model ID

这是**最关键**的一步！默认的 `doubao-seed-2-0-lite-260215` 不能直接使用！

1. 进入火山方舟控制台：
   https://console.volcengine.com/ark/region:ark+cn-beijing

2. 点击"在线推理"服务

3. 选择 doubao-seed-2.0-lite 模型（或你想用的其他版本）

4. 点击"创建连接"或"创建推理接入点"

5. 输入名称，点击"创建"

6. **等待状态变为"运行中"**

7. **复制生成的接入点 ID**（格式类似：`ep-xxxxxxxxxxxxxx`）

---

### 第 3 步：更新 .env 文件

编辑项目根目录下的 `.env` 文件，填入正确信息：

```env
# API Key - 从 API密钥管理 页面复制
ARK_API_KEY=你的完整API Key

# Model ID - 从在线推理服务复制你的接入点ID
MODEL_ID=ep-xxxxxxxxxxxxxx

# 端口
PORT=3000
```

---

### 第 4 步：验证配置

运行测试脚本验证：

```bash
python simple_test.py
```

如果看到 `SUCCESS!` 和响应内容，说明配置成功！

---

### 第 5 步：重启服务

1. 在运行服务的终端按 `Ctrl+C` 停止

2. 重新启动：

```bash
python app.py
```

3. 刷新浏览器，重新测试！

---

## 🔧 常见问题排查

### Q: API Key 应该是什么格式？
A: 正确的 API Key 格式通常是 `ak-` 开头的长字符串，类似：
```
ak-1234567890abcdefghijklmnopqrstuv
```

### Q: Model ID 在哪里获取？
A: 不是模型名称！是你**自己部署后**生成的接入点 ID，格式是：
```
ep-xxxxxxxxxxxxxx
```

### Q: 部署模型需要花钱吗？
A: 通常有免费额度，具体请查看火山方舟控制台的计费说明。

---

## 📞 还是不行？

如果按上述步骤操作后还有问题，请：

1. 运行 `python simple_test.py`
2. 把完整的输出结果发给我
3. 我会帮你进一步诊断！

---

## ✅ 配置成功后

配置成功后，应用会：
- 接受 5-10 张图片
- 调用豆包大模型分析
- 生成人物画像和交友建议
- 友好的用户界面展示结果

祝使用愉快！🎉
